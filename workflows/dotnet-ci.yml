# Using it from any repository:
# name: CI

# on:
#   push:
#     branches:
#       - main
#       - develop
#   pull_request:
# jobs:
#   ci:
#     uses: HawkeyePlatform/.github/.github/workflows/dotnet-ci.yml@main
#     with:
#       solution: Hawkeye.sln
#       coverage-threshold: 100

name: .NET CI

on:
  workflow_call:
    inputs:
      dotnet-version:
        type: string
        default: "9.0.x"

      solution:
        type: string
        default: "."

      coverage-threshold:
        type: number
        default: 100

permissions:
  contents: read

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ inputs.dotnet-version }}

      - name: Restore
        run: dotnet restore ${{ inputs.solution }}

      - name: Build
        run: dotnet build ${{ inputs.solution }} --configuration Release --no-restore

      - name: Test with Coverage
        run: |
          dotnet test ${{ inputs.solution }} \
            --configuration Release \
            --no-build \
            --collect:"XPlat Code Coverage" \
            --results-directory TestResults

      - name: Install ReportGenerator
        run: dotnet tool install --global dotnet-reportgenerator-globaltool

      - name: Generate Coverage Report
        run: |
          reportgenerator \
            -reports:"TestResults/**/coverage.cobertura.xml" \
            -targetdir:"CoverageReport" \
            -reporttypes:"JsonSummary"

      - name: Verify Coverage
        shell: bash
        run: |
          COVERAGE=$(jq '.summary.linecoverage' CoverageReport/Summary.json)

          echo "Coverage: ${COVERAGE}%"

          THRESHOLD=${{ inputs.coverage-threshold }}

          awk -v c="$COVERAGE" -v t="$THRESHOLD" \
            'BEGIN { exit (c >= t ? 0 : 1) }'
